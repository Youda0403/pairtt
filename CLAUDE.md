# 프로젝트: Pairframe (페어 포스터 메이커)

## 목적
레트로 다이너 스타일 포스터 템플릿에 사진 한 장과 두 사람의 정보를 넣어 만들고,
고화질 PNG로 저장하는 개인용 웹앱. 주 사용처는 폰 브라우저.
참조 디자인(체크무늬 테두리 · Pairname 스크립트 로고 · 영수증 메뉴 · 두 장의 티켓형
캐릭터 카드)을 최대한 그대로 재현하되, 기능은 최소한으로만 둔다.

## 아키텍처

빌드 없음, 서버 없음, 외부 라이브러리 없음. 정적 파일 4개(+폰트)가 전부다.

```
index.html   UI 골격 + 포스터 전체(인라인 SVG, viewBox 0 0 1000 1500)
style.css    편집 화면 스타일 (포스터 색상은 여기 없음)
app.js       상태·렌더·사진조작·색상주입·PNG 내보내기
fonts/       Pacifico / Caveat / Nunito latin 서브셋 (OFL 1.1)
```

`python3 -m http.server` 로 띄워서 확인한다. `file://`로 열면 폰트 임베드 fetch가 막힌다.

## 핵심 설계

### 포스터는 SVG 하나
화면에 보이는 것과 저장되는 것이 **같은 SVG**다. 미리보기와 결과물이 어긋날 여지가 없다.
디자인 좌표는 1000 × 1500 설계 공간. 사진 프레임은 `FRAME = {196, 278, 668, 590}`이고
`app.js`와 `index.html`의 `clip-photo` / `photoHit`이 같은 값을 쓴다. **셋을 같이 고칠 것.**

### 색상 주입
SVG 안에 `<style id="theme-style">`이 있고 `app.js`의 `themeCss()`가 통째로 써넣는다.
도형에는 색을 직접 박지 않고 `.f-*`(fill) / `.s-*`(stroke) 클래스만 쓴다.
이 `<style>`이 SVG 내부에 있어서 저장할 때 그대로 딸려 나간다.

사용자 조절 항목은 6개(paper / red / p1 / p2 / green / accent)뿐이고,
그림자 레드·사진 안내문 초록·종이 얼룩색은 `deepOf` / `inkOf` / `grainOf`로 자동 계산한다.
항목을 늘리기 전에 계산으로 유도할 수 있는지 먼저 볼 것.

### 고화질 저장
SVG 복제 → 폰트를 base64 `@font-face`로 심기 → 주석 제거 → `<img>`로 읽어
캔버스에 3배(3000 × 4500)로 그리고 `toBlob`. 외부 라이브러리 없음.

**함정:** 직렬화된 SVG는 XML로 파싱된다. 주석 안 연속 하이픈(`--`)이 있으면
저장이 통째로 실패한다(실제로 한 번 겪음). `stripComments()`가 방어하지만,
SVG에 새 주석을 넣을 때 `<!-- ----- 제목 ----- -->` 같은 구분선을 쓰지 말 것.

### 텍스트 배치
글자 폭을 `getComputedTextLength()`로 재서 처리한다.
- `fitText()` — 폭을 넘으면 font-size를 줄인다 (페어 이름, 캐릭터 이름, 항목 값)
- `layoutRow()` — 라벨 폭을 재서 콜론 위치와 점선 시작점을 잡는다.
  `LIKE`↔`DISLIKE`처럼 길이가 바뀌어도 줄이 겹치지 않는다.
- 페어 이름 옆 별은 이름 길이에 따라 `render()`에서 위치를 다시 잡는다.

font-size를 CSS 클래스로 고정하면 `fitText`가 못 이긴다. 반드시 `el.style.fontSize`로 쓸 것.

### 캐릭터 카드 항목
`NAME / LIKE / SPECIAL`이 아니라 **`AGE / JOB / LIKE`** 다.
이름은 카드 상단 타이틀이 이미 담당하므로 항목에서 뺐다.
`LIKE`는 `DISLIKE`로 토글되고, **값에는 상대 캐릭터의 이름이 자동으로 들어간다.**

### 사진 조작
`#photoHit` 투명 사각형에서만 포인터 이벤트를 받는다. 여기에만 `touch-action: none`을
걸어서 포스터 나머지 영역은 정상적으로 스크롤된다. 드래그=이동, 휠·두 손가락=확대.
확대·이동 범위는 사진이 항상 프레임을 덮도록 `placePhoto()`에서 클램프한다.

### 업로드 이미지
사진 2200px / 스티커 800px로 줄여서 data URI로 담는다(스티커는 투명 유지를 위해 PNG).
원본을 그대로 두면 저장이 느려지고 `localStorage` 용량을 넘긴다.

### 상태 저장
전체 상태를 `localStorage`(`pairframe.v1`)에 넣는다. 입력할 때마다 수 MB를 직렬화하지
않도록 400ms 디바운스를 걸었고, 용량 초과 시 이미지를 뺀 축약본으로 재시도한다.

## 현재 상태
- 기능 전부 구현·검증 완료. Playwright로 데스크톱/모바일 렌더, 사진 업로드·드래그·확대,
  스티커 교체, 색상 프리셋, 3000×4500 PNG 저장, 새로고침 후 복원까지 실제로 확인함.
- 배포는 아직 안 함. 정적 호스팅에 그대로 올리면 된다.

## 코딩 시 반드시 지킬 규칙 (사용자 고정 선호)
1. 요청한 수정사항을 적용한 뒤, 전체 코드에 다른 문제가 없는지 검토하고 나서 답변할 것
2. 어디가 문제였고 어디를 고쳤는지 명확하게 설명할 것
3. 요청받지 않은 다른 부분은 절대 임의로 수정하지 말 것

## 앞으로 확장 여지 (요청 있을 때만)
- 스티커 위치·크기 조절 (지금은 고정 슬롯에 교체만)
- 포스터 문구(Better Together, TODAY'S MENU 등) 편집
- 저장 배율 선택 (지금은 3배 고정)
- 템플릿 여러 종
