# 프로젝트: Pairframe (페어 다이너 메뉴판 메이커)

## 목적
1950~60년대 미국 다이너 메뉴판 컨셉으로, 두 사람을 "오늘의 스페셜 메뉴"처럼 소개하는
세로형 포스터를 만들고 고화질 PNG로 저장하는 개인용 웹앱. 주 사용처는 폰 브라우저.

디자인 우선순위는 **페어 이름 → 메인 사진 → 캐릭터 카드 → 관계 정보 → 장식** 순이다.
장식이 콘텐츠보다 눈에 띄면 안 된다.

## 아키텍처

빌드 없음, 서버 없음, 외부 라이브러리 없음. 정적 파일 3개(+폰트)가 전부다.

```
index.html   UI 골격 + 포스터 전체(인라인 SVG, viewBox 0 0 1000 1500)
style.css    편집 화면 스타일 (포스터 색상은 여기 없음)
app.js       상태·렌더·사진조작·색상주입·PNG 내보내기
fonts/       라틴: Pacifico / Caveat / Nunito, 한글: Jua / Gaegu / Gothic A1 (OFL 1.1)
```

`python3 -m http.server` 로 띄워서 확인한다. `file://`로 열면 폰트 임베드 fetch가 막힌다.

## 핵심 설계

### 포스터는 SVG 하나
화면에 보이는 것과 저장되는 것이 **같은 SVG**다. 미리보기와 결과물이 어긋날 여지가 없다.
디자인 좌표는 1000 × 1500 설계 공간, 세로 중심축은 x = 500.

**포스터는 반응형으로 재배치되지 않는다.** 인쇄물처럼 비율이 고정된 작품이고,
화면 폭에 맞춰 통째로 축소만 된다(저장본과 구도가 같아야 하므로). 모바일에서 세로로
쌓이는 것은 편집 패널이다.

### 세로 흐름 (위에서 아래로)
| y | 내용 |
|---|---|
| 0–24 | 얇은 체커보드 띠 (12px 칸, 2줄). 절제된 시그니처 요소 |
| 40–105 | 상단 문구 + 그린 스우시, 오른쪽에 EST 배지 |
| ~238 | 페어 이름 (그린 그림자 + 크림 아웃라인 + 레드 = 다이너 간판) |
| 257–301 | `DINER & GRILL` 리본 |
| 326–368 | `TODAY'S SPECIAL` 배너 (사진 카드 위에 얹힌다) |
| 348–712 | 메인 사진 카드 (전체 높이의 약 24%) |
| 742–784 | `OUR SPECIAL PAIR` 배너 |
| 800–1120 | 캐릭터 카드 2장 (좌우 대칭, 여백 60px) |
| ~1162 | `ALWAYS TOGETHER` |
| 1188–1392 | `TODAY'S MENU` 가로 카드 (그린) — 아래쪽 시각적 휴식 구간 |
| ~1440 | 하단 문구 + xoxo 도장 |
| 1476–1500 | 체커보드 띠 |

### 그리는 순서 = 겹치는 순서
`TODAY'S SPECIAL` 배너는 사진 카드보다 **뒤에** 적어야 카드 위로 올라온다.
스티커 3종도 마찬가지로 맨 뒤에 둔다(옮긴 스티커가 다른 요소 뒤로 숨지 않게).

### 색상 주입
SVG 안에 `<style id="theme-style">`이 있고 `app.js`의 `themeCss()`가 통째로 써넣는다.
도형에는 색을 직접 박지 않고 `.f-*`(fill) / `.s-*`(stroke) 클래스만 쓴다.
이 `<style>`이 SVG 내부에 있어서 저장할 때 그대로 딸려 나간다.

사용자 조절 항목은 6개다.

| 키 | 역할 | 기본값 |
|---|---|---|
| paper | 크림 배경 | `#F7EEDB` |
| red | 메인 브랜딩·테두리·제목 | `#D9362E` |
| green | 서브 브랜딩·간판 그림자·메뉴 카드 | `#276B4A` |
| p1 / p2 | 캐릭터 01 / 02 구분용 (핑크·블루) | `#E56B7D` / `#4E91C9` |
| accent | 배너 속 별 (작은 하이라이트 전용) | `#F2B84B` |

나머지는 자동 계산한다 — `sheetOf`(카드 종이) / `inkOf`(따뜻한 먹색, 검정 대신) /
`lightOf`(사진 자리 연두) / `grainOf`(얼룩) / `onColor`(색 띠 위 글자).
항목을 늘리기 전에 계산으로 유도할 수 있는지 먼저 볼 것.

**음식 색은 테마를 따르지 않는다.** `.f-bun` `.f-patty` `.f-cheese` `.f-lettuce`
`.f-sesame` `.f-fry` `.s-food` 는 고정값이다. 포장(감자튀김 봉투 체크·접시)만 테마를 따른다.

### 배너·리본은 JS가 글자 폭에 맞춰 그린다
`ribbon()`(양끝 뾰족) / `tab()`(오른쪽만 뾰족) / `starRibbon()`(별-글자-별 가운데 정렬)이
`getComputedTextLength()`로 잰 폭에 맞춰 `path`의 `d`를 만든다.
문구를 바꿔도 띠 크기가 따라간다.

### 사진 세 자리
`FRAMES` 에 메인 사진과 캐릭터 초상 2장이 같은 모양으로 들어 있다.
`box` 값은 index.html 의 `clip-path` / hit 사각형과 **반드시 같아야 한다.**
`placeFrame()` 하나가 셋을 다 처리하고, cover 방식으로 채운 뒤 프레임을 벗어나지 않게 클램프한다.

### 폰트 (라틴 + 한글)
역할별로 라틴 폰트를 먼저, 한글 폰트를 뒤에 둔 `font-family` 스택을 쓴다.
라틴 폰트에 한글 글리프가 없으니 한글을 입력하면 자동으로 한글 폰트로 넘어간다.

| 역할 | 라틴 | 한글 |
|---|---|---|
| `.disp` 페어 이름 | Pacifico | Jua |
| `.hand` 손글씨 | Caveat | Gaegu |
| `.sans` 라벨·머리글 | Nunito | Gothic A1 |

한글 폰트는 한 벌에 230~460KB라 **쓰일 때만** 다룬다.
- 화면: `ensureKrFonts()`가 한글이 있는 역할만 받고 다시 그린다(폭을 다시 재야 배치가 맞는다).
- 저장: `neededKrFonts(clone)`이 고른 것만 base64로 심는다. 라틴 3종은 항상 심는다.

### 고화질 저장
SVG 복제 → 히트 영역 제거 → 필요한 폰트만 base64 `@font-face`로 심기 → 주석 제거 →
`<img>`로 읽어 캔버스에 3배(3000 × 4500)로 그리고 `toBlob`. 외부 라이브러리 없음.

**함정:** 직렬화된 SVG는 XML로 파싱된다. 주석 안 연속 하이픈(`--`)이 있으면
저장이 통째로 실패한다(실제로 한 번 겪음). `stripComments()`가 방어하지만,
SVG에 새 주석을 넣을 때 `<!-- ----- 제목 ----- -->` 같은 구분선을 쓰지 말 것.

### 텍스트 배치
글자 폭을 `getComputedTextLength()`로 재서 처리한다.
- `fitText()` — 폭을 넘으면 font-size를 줄인다
- `layoutRow()` — 라벨 폭을 재서 콜론 위치와 점선 시작점을 잡는다
- 배너/장식 위치도 폭에서 계산한다

font-size를 CSS 클래스로 고정하면 `fitText`가 못 이긴다. 반드시 `el.style.fontSize`로 쓸 것.

### 캐릭터 카드
`♥ CHARACTER 01` 머리 띠 + 초상 사진 + `NAME / AGE / JOB / LIKE` + `TODAY'S ORDER` 띠.
사원증 느낌(ID 번호·바코드·목걸이 구멍)은 메뉴판 컨셉과 안 맞아 전부 뺐다.

`TODAY'S ORDER` 는 **비워 두면 상대 캐릭터의 이름이 자동으로 들어간다.** 직접 쓰면 그 값을 쓴다.

### 사진·스티커 표시 (중요)
`<image>`의 `display`는 **반드시 `'inline'` / `'none'` 값으로** 지정한다.
`style.display = ''`로 인라인 스타일을 지우면 스타일시트 규칙이 다시 이겨버린다.
실제로 `style.css`의 `display:none` 때문에 화면에서는 사진이 안 보이고 저장본에서만
보이는 버그가 있었다(저장본은 외부 CSS가 안 붙는다).

### 파일 선택
`<label>` 안에 파일 `<input>`을 겹쳐 두면 모바일에서 선택창이 두 번 열리거나 안 열린다.
숨긴 `<input>` 두 개(사진용 `#in-photo`, 스티커용 `#in-sticker`)를 두고 버튼에서
`.click()` 하며, 어느 자리를 눌렀는지 `pendingChar` / `pendingSlot` 에 기억한다.

### 사진·스티커 조작
투명한 히트 사각형에서만 포인터 이벤트를 받는다. `bindGesture()` 하나로 끌기(이동)와
두 손가락·휠(확대)을 함께 처리한다. 사진 1~4배, 스티커 0.3~3배.

**모바일 사파리는 SVG 요소의 `touch-action`을 무시할 때가 있다.** 그래서 끄는 동안
`dragging` 플래그를 세우고 포스터의 `touchmove`를 `preventDefault()` 한다.

### 업로드 이미지
메인 사진 2200px / 캐릭터 초상 1100px / 스티커 900px로 줄여서 data URI로 담는다
(스티커는 투명 유지를 위해 PNG). 원본을 그대로 두면 저장이 느려지고 용량을 넘긴다.

### 편집 가능한 항목
페어 이름 · 부제 · 설립 연도 · 상단 문구 / 메인 사진 /
캐릭터 01·02 (사진 · NAME · AGE · JOB · LIKE · TODAY'S ORDER) /
메뉴 4줄 · 오른쪽 메모(`/` 로 줄바꿈, 3줄) · 가운데 문구 · 맨 아래 문구 / 스티커 3종 / 색상 6종.

### 상태 저장
전체 상태를 `localStorage`(`pairframe.v3`)에 넣는다. 입력할 때마다 수 MB를 직렬화하지
않도록 400ms 디바운스를 걸었고, 용량 초과 시 이미지를 뺀 축약본으로 재시도한다.

## 현재 상태
- 기능 전부 구현·검증 완료. Playwright로 데스크톱/모바일 렌더, 사진 3장 업로드·드래그·확대,
  스티커 교체, 긴 이름 오버플로 검사, 색상 프리셋, 3000×4500 PNG 저장,
  새로고침 복원, 전체 초기화까지 실제로 확인함.
- 배포는 아직 안 함. 정적 호스팅에 그대로 올리면 된다.

## 코딩 시 반드시 지킬 규칙 (사용자 고정 선호)
1. 요청한 수정사항을 적용한 뒤, 전체 코드에 다른 문제가 없는지 검토하고 나서 답변할 것
2. 어디가 문제였고 어디를 고쳤는지 명확하게 설명할 것
3. 요청받지 않은 다른 부분은 절대 임의로 수정하지 말 것

## 앞으로 확장 여지 (요청 있을 때만)
- 곰 마스코트 되살리기 (메뉴판 개편 때 뺐다)
- 캐릭터 카드 머리 띠 문구 편집
- 스티커 회전 / 슬롯 추가
- 저장 배율 선택 (지금은 3배 고정)
- 템플릿 여러 종
