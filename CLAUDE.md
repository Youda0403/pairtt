# 프로젝트: Pairframe (페어 다이너 메뉴판 메이커)

## 목적
1950~60년대 미국 다이너 메뉴판 컨셉으로, 두 사람을 "오늘의 스페셜"처럼 소개하는
세로형 포스터를 만들고 고화질 PNG로 저장하는 개인용 웹앱. 주 사용처는 폰 브라우저.

**다이너는 무대고 페어가 주인공이다.** 장식이 두 사람보다 눈에 띄면 안 된다.

## 아키텍처

빌드 없음, 서버 없음, 외부 라이브러리 없음. 정적 파일 3개(+폰트·그림)가 전부다.

```
index.html   UI 골격 + 포스터 전체(인라인 SVG, viewBox 0 0 1000 1500)
style.css    편집 화면 스타일 (포스터 색상은 여기 없음)
app.js       상태·렌더·사진조작·색상주입·PNG 내보내기
fonts/       라틴: Alfa Slab One / Yellowtail / Oswald, 한글: Jua / Gaegu / Gothic A1 (OFL 1.1)
art/         음식 그림 3종 (버거·피자·밀크셰이크) 사용자가 직접 그린 PNG
```

`python3 -m http.server` 로 띄워서 확인한다. `file://`로 열면 폰트·그림 임베드 fetch가 막힌다.

## 핵심 설계

### 포스터는 SVG 하나
화면에 보이는 것과 저장되는 것이 **같은 SVG**다. 미리보기와 결과물이 어긋날 여지가 없다.
디자인 좌표는 1000 × 1500 설계 공간, 세로 중심축은 x = 500.

**포스터는 반응형으로 재배치되지 않는다.** 인쇄물처럼 비율이 고정된 작품이고,
화면 폭에 맞춰 통째로 축소만 된다. 모바일에서 세로로 쌓이는 것은 편집 패널이다.

### 겹쳐진 세 겹 (제일 중요)
레퍼런스는 **틀 안에 또 틀이 있고, 안쪽 틀이 사진을 잘라 먹는다.** 이 순서를 지켜야 한다.

```
① 종이 + 인쇄 테두리 + 체커 띠        ← 바깥 틀
② 간판 · 원형 초상 · 가운데 그림       ← 사진 층 (아래쪽이 잘린다)
③ 메뉴 틀 (불투명 종이색 + 빨간 테두리) ← 안쪽 틀. ②의 아랫부분을 덮는다
④ 음식 그림 → 메뉴 글자                ← 틀 위에 얹힌다
⑤ 종이 질감 두 겹                      ← 전부를 덮는다(사진 포함)
```

가운데 그림 상자는 414~854 인데 메뉴 틀이 774 에서 시작해 **아래 80 을 덮는다.**
이 겹침이 없으면 요소들이 "보이지 않는 사각형에 갇힌" 것처럼 보인다. 상자를 딱 맞추지 말 것.

**사진은 액자까지 통째로 ② 층이다.** 액자를 메뉴 틀보다 뒤에 그렸더니 액자 아랫변과 틀 윗선이
겹쳐 두 줄이 엇갈려 보였다. 사진 층 안에 두면 아랫변이 틀에 깔끔하게 잘린다.

**가로로도 겹치면 안 된다.** 원형 초상(…236 / 764…)과 가운데 그림(258~742)은 22 를 띄웠다.
초상을 키우거나 그림 상자를 넓히려면 이 간격부터 확인할 것. 예전에 원 테두리가 액자 선을 뚫었다.

### 구성
| y | 내용 |
|---|---|
| 15–1485 | 빨간 인쇄 테두리 2겹 + 안쪽 위아래 체커보드 띠(40~64 / 1436~1460) |
| 78–154 | 띠 문구 — 알약 간판(`ARCH_CY` 116) + 종이색 **바깥** 테두리 + 양옆 작은 별 |
| 130–337 | **페어 이름** — 호 위의 `textPath`. **잉크 꼭대기가 `NAME_TOP`(136)에 고정**, 기본 224px |
| ~310–362 | 알약 문구 (`MENU`). baseline + `PILL_DROP`(18). **맨 나중에 그려 이름 밑동을 덮는다** |
| 160 | 오른쪽 위 별폭발 배지 (`#ic-burst`, 44각). 체커 띠·괘선에서 10 이상 떨어뜨린다 |
| 414–854 | 가운데 그림 x 258~742 (아래 80 은 메뉴 틀에 가린다) + 켜고 끄는 액자 |
| 406–582 | 원형 초상 2장 (r88, cx 148 / 852) + **원 바깥 아래를 도는 이름 호**(r122) |
| 774–1364 | **메뉴 틀** + 3단. 가운데 열은 자기 상자(`#midBox`)를 하나 더 두른다 |
| 675~1400 | 음식 그림 3종. 메뉴 틀의 위·아래 모서리를 물고 앉는다 |
| 1406 | 바닥 장식 손글씨(34px) + 양옆 별 |

**간판 셋은 순서대로 겹쳐 한 덩어리로 읽힌다** — 띠(78~154) · 페어명(130~) · 알약(~310~362).
페어명 꼭대기가 띠의 아랫변을 **24** 밟고, 알약이 페어명의 밑동·초록 그림자·종이색 테두리를
통째로 덮는다. 그리기 순서는 **띠 → 배지 → 페어명 → 알약**이고, 알약이 맨 위다.

**빨강 위의 빨강은 안 보인다.** 겹침이 10 남짓이면 종이색 테두리만 띠 아랫변에 살짝 자국을
남겨서 "띠 아랫변이 울퉁불퉁한" 것처럼 보이지 겹친 걸로 안 읽힌다. 글자 옆선까지 띠 안으로
들어가야(≈20 이상) 비로소 겹쳐 보인다. 그래서 띠 높이를 66 → 76 으로 되돌리고
대신 **폭을 넓혔다**(`padX` 96) — 높이만 키우면 다시 뚱뚱해 보인다. 비율로 볼 것.
한때 알약을 안 겹치게 떼거나 메뉴 틀로 내려보낸 적이 있는데 둘 다 따로 노는 것처럼 보였다.
**애매하게 스치는 게 제일 나쁘다 — 확실히 덮을 것.** 덮는 쪽에 종이색 테두리가 있으면 깔끔하다.

**겹치는 빨간·초록 덩어리에는 종이색 테두리를 두른다.** 알약(`s-paper` 7)·띠(7)·페어명(12).
위에 얹히는 쪽이 테두리로 아래를 잘라 줘야 겹친 자리가 깨끗하다.
띠는 안쪽에 얇은 실선을 그었다가 걷어냈다 — 가는 선 하나는 시안처럼 보이지 않고 거슬리기만 한다.
**모양을 채우고 바깥에 테두리를 두르는 쪽**이 맞다.

**위아래 바깥 여백을 맞춘다.** 체커 띠 아래(64) → 띠 위(78) = **14**,
바닥 글자 아래(1421.9) → 체커 띠 위(1436) = **14.1**. 바닥 글자는 baseline 을 박지 않고
`getBBox` 로 재서 빈 띠(`FOOT_CY`) 한가운데에 맞춘다 — 손글씨는 위아래 여백이 제각각이라
baseline 을 고정하면 반드시 한쪽으로 쏠린다.

**띠·알약 글자의 baseline 은 글자 크기에서 계산한다**(`cy + fs*0.35`). 고정값을 쓰면
`fitText` 가 글자를 줄였을 때만 위로 떠서 위아래 정렬이 어긋난다. 섹션 머리와 같은 규칙이다.

띠는 곧은 알약 모양이고 글자도 곧다(호가 아니다). 페어명만 `NAME_ARC` 로 살짝 휜다 —
이름이 길어져도 곡률이 같아야 하고, 곧게 두면 긴 이름이 그냥 작아지기만 한다.

**페어명은 baseline 이 아니라 잉크 꼭대기(`NAME_TOP` 136)를 고정한다.** 그래야 한글(162px)이든
긴 이름(줄어든 크기)이든 띠를 밟는 깊이가 24 로 같다. baseline 을 고정했더니 작아진 글자는
띠에서 뚝 떨어져 겹침이 사라졌다. **알약은 baseline + `PILL_DROP` 을 따라다니므로** 위를
고정해도 밑동이 안 뜬다 — 예전에 위 고정을 포기했던 이유가 이거였는데, 알약을 딸려 보내면 된다.
잉크 높이는 글자 크기 × `CAP_LAT`(0.814) / `CAP_KR`(0.835) 로 잡는다. 재서 얻은 값이다.

**바닥 장식은 메뉴 틀(1364)과 체커 띠(1436) 사이 72 안에 산다.** 이 틈이 좁으면 글자가
두 선 사이에 낀 것처럼 보인다. 그래서 사진 층과 메뉴 틀을 통째로 26 올려 이 자리를 만들었다.
바닥에 글자를 넣을 거면 **위쪽 전체를 함께 올려야** 한다. 글자만 밀어 넣지 말 것.

띠를 고정 길이로 두면 짧은 문구가 붉은 소시지 위에 뜬다. 그래서 **띠도 글자 폭에서 길이를 잡는다**(`pill()`).

**배지는 페어명보다 먼저 그린다.** 이름이 길어지면 오른쪽 끝이 배지 자리까지 오는데,
배지가 뒤에 있으면 이름을 덮어 버린다. 무대의 주인공은 이름이다.

**장식 손글씨는 메뉴 틀 아래 빈 띠 하나뿐이다.** 예전에 세 개를 요소 사이 틈에 끼워 넣었다가,
왼쪽 위 한 개까지 결국 전부 걷어냈다. 빈 곳이라고 다 채우면 안 된다.

### 메뉴판 본문은 고정값이다
`MENU` 상수에 6개 섹션 29줄이 박혀 있다. **사용자가 입력하는 줄은 딱 하나** —
`SPECIAL PAIR SET` 의 첫 줄(`'@pair'` 표시)이고, 비우면 `이름 + 이름` 이 자동으로 들어간다.
메뉴 전체를 입력받게 만들면 편집 패널이 폼 30줄짜리가 된다. 늘리지 말 것.

**네모 상자는 `SPECIAL PAIR SET` 한 섹션만 두른다**(`sec.box`). 윗변이 **간판 한가운데(845)에서
시작해** 마지막 줄 아래 `BOX_PAD`(28)에서 닫힌다(845~1046) — 간판이 상자에 얹힌 것처럼 보인다.
`DRINKS` 는 상자 밖이다. 원래 둘을 같이 묶었는데 서로 상관없는 섹션이라 묶을 이유가 없었다.
상자 다음 섹션까지는 `SEC_GAP` 에 `BOX_GAP`(16)을 더해 띄운다.

**섹션 머리는 높이가 달라도 중심이 `HEAD_MID`(19)로 같다.** `drawHead` 가 `top - (h-38)/2` 에서
그리고 `19 + h/2` 를 돌려준다. 이걸 안 하면 두 줄짜리 `plaque` 만 30 처져 보인다 —
전에는 상자를 내용 높이에서 만들고 틀 한가운데 정렬했는데, 그러면
`P = 틀중심 - H/2 + 7 + h/2` 이고 `H ≈ 상수 + h` 라 **간판을 줄여도 중심이 874 에 박혔다.**
지금은 세 열의 머리 중심이 전부 **845** 로 같다.

**열마다 `MENU_TOP`(826)에서 시작해 같은 간격으로 쌓는다.** 섹션 머리 아래 `HEAD_GAP`(36),
줄 간격 `ROW_PITCH`(33), 섹션 사이 `SEC_GAP`(50) — 좌표를 손으로 박지 않아 여백이 늘 같다.
머리와 항목이 붙으면 표처럼 답답해 보인다. 이 값들을 다시 좁히지 말 것.

**초상 이름은 원 바깥 아래를 도는 호에 얹는다**(`#arc-c1` / `#arc-c2`, r122).
두 번 갈아엎은 자리다 — 빨간 둥근 사각 이름표는 멀리서 보면 섹션 머리와 구분이 안 갔고,
원 안쪽을 가로지르는 띠는 **얼굴을 가려 버렸다.** 지금은 얼굴을 비우고 형태도 다르다.
글자는 baseline 에서 **위로** 자라니 반지름은 `원 r + 테두리 + 글자 높이` 만큼 띄워야 한다.
120 으로 잡았다가 글자가 원 안으로 들어가는 버그가 있었다.

**원형 초상은 통째로 살짝 기울여 둔다**(`rotate(-4 148 494)` / `rotate(4 852 494)`).
원은 돌려도 그대로라 실제로 기울어 보이는 것은 안쪽 이름 띠다. 좌우가 반대로 기울어야
둘이 마주 보는 느낌이 난다. 회전 중심을 원 중심에 두어야 clip 과 hit 사각형이 어긋나지 않는다.

**섹션 머리는 네 가지 모양**이다(`style`). 같은 알약이 여섯 번 반복되면 표처럼 보인다.
- `slant` 살짝 기운 둥근 사각 (MAIN / SIDE / DESSERTS)
- `band` 반듯한 사각 (DRINKS)
- `plaque` 위아래가 부푼 간판 + 안쪽 종이색 괘선 + 별 두 개 (SPECIAL PAIR SET).
  타원이었는데 시안처럼 간판으로 바꿨다. 두 줄이 들어가므로 **높이 76 · 글자 25 · 줄 간격 `fs+9`**.
  68 은 글자가 변에 닿아 옹졸했고 90 은 혼자 너무 컸다. 지금이 가운데다.
  상자 윗변이 이 간판 한가운데에서 시작한다
- `ribbon` 양끝이 뾰족한 리본 (EXTRA)

`dx` 로 머리를 좌우로 밀 수 있다. MAIN 은 +54, DESSERTS 는 -54 — 그 자리에 음식 그림이 앉는다.

**머리 글자의 baseline 은 글자 크기에서 계산한다**(`cy + fs*0.35`). 고정값을 쓰면
두 줄짜리(`oval`)만 위로 떠서 위아래 정렬이 어긋난다. 실제로 그 버그가 있었다.

### 장식은 배경 무늬 뾰족별 + 배지 하나
`#ic-spark` (8각 별, **테두리만**) 세 개를 **아주 크게(scale 1.6~2.9), 아주 옅게(.055~.075),
선은 얇게(2.2~3)** 맨 뒤에 깔아 배경 무늬로 쓴다. 작은 별을 여기저기 흩뿌려 봤지만 잔가지로만 보였다.
크게 깔면 무늬가 되고 작게 뿌리면 쓰레기가 된다. 개수를 늘리지 말 것.

### 종이 질감
필터 두 겹을 맨 위에서 전체에 덮는다. **사진도 함께 덮여야 종이 위에 인쇄된 것으로 보인다.**
- `#fx-fiber` 잔 알갱이 (`feTurbulence` baseFrequency .9)
- `#fx-stain` 넓고 성긴 얼룩 (baseFrequency .012). opacity 를 .3 이상 올리면 물자국처럼 된다.

올린 사진에는 `#fx-tone` 을 따로 건다 — 채도 0.78 로 낮추고 **종이 색을 `TONE_MIX`(0.16)만큼 덮는다.**
`out = (1-k)*in + k*paper` 를 `feComponentTransfer` 의 linear(slope/intercept)로 그대로 쓴 것이고,
값은 `applyTheme()` 이 `S.colors.paper` 에서 계산해 `#tone-r/g/b` 에 써넣는다.
**색을 박아 두면 안 된다** — 어두운 프리셋에서 사진만 크림색으로 따뜻해져 배경과 따로 논다.
요즘 사진을 그대로 두면 이 종이 위에서 혼자 현대적으로 뜬다.

`<img>` 로 캔버스에 그릴 때도 SVG 필터는 그대로 렌더된다(저장본에서 확인함).

### 음식 그림은 래스터다
`art/*.png` 는 사용자가 직접 그린 그림이다. 분필 질감이 "인쇄 잉크가 번진" 느낌을 만든다.
- **크게 그려 섹션 머리에 겹친다.** 빈틈에 작게 끼워 넣으면 구성이 옹색해진다.
- 그래서 음식 그림은 `#menu` 보다 **앞에** 적는다. 그래야 섹션 글자가 그림 위로 올라와
  겹쳐도 읽힌다. 순서를 뒤집으면 그림이 글자를 덮는다.
- 다만 **항목 줄(이름·가격)과는 겹치지 않게** 세로 위치를 잡는다. 줄 글자는 작아서
  그림 위에 놓이면 못 읽는다.
- 도형을 결합해 SVG로 다시 그리지 말 것. 예전에 그렇게 만들었다가 전부 걷어냈다.
- 스티커 슬롯 3칸의 **기본값**이라, 사용자가 투명 PNG를 올리면 그 자리에 대신 들어간다.
- 색이 6가지뿐이라 32색 팔레트 PNG로 저장했다(셋 합쳐 96KB).
- **테마 색을 따르지 않는다.** 어두운 프리셋에서는 크림/베이지가 뜬다. 의도된 한계다.

### 색상 주입
SVG 안에 `<style id="theme-style">`이 있고 `app.js`의 `themeCss()`가 통째로 써넣는다.
도형에는 색을 직접 박지 않고 `.f-*`(fill) / `.s-*`(stroke) 클래스만 쓴다.
이 `<style>`이 SVG 내부에 있어서 저장할 때 그대로 딸려 나간다.

조절 항목은 4개뿐이다 — paper(크림) · red(메인) · green(서브) · accent(별).
나머지는 자동 계산한다: `sheetOf`(한 톤 밝은 종이) / `inkOf`(따뜻한 먹색, 검정 대신) /
`grainOf`(얼룩) / `onColor`(색 띠 위 글자). 항목을 늘리기 전에 계산으로 유도할 수 있는지 볼 것.

### 폰트 (라틴 + 한글)
역할별로 라틴 폰트를 먼저, 한글 폰트를 뒤에 둔 `font-family` 스택을 쓴다.
라틴 폰트에 한글 글리프가 없으니 한글을 입력하면 자동으로 한글 폰트로 넘어간다.

| 역할 | 라틴 | 한글 |
|---|---|---|
| `.slab` 페어 이름·큰 제목 | Alfa Slab One | Jua |
| `.script` 손글씨 | Yellowtail | Gaegu |
| `.cond` 메뉴·라벨 | Oswald 600 | Gothic A1 |

한글 폰트는 한 벌에 230~460KB라 **쓰일 때만** 다룬다.
- 화면: `ensureKrFonts()`가 한글이 있는 역할만 받고 다시 그린다(폭을 다시 재야 배치가 맞는다).
- 저장: `neededKrFonts(clone)`이 고른 것만 base64로 심는다. 라틴 3종은 항상 심는다.

### 고화질 저장
SVG 복제 → 히트 영역 제거 → **`art/` 그림을 data URI 로 치환**(`inlineArt`) →
필요한 폰트만 base64 `@font-face` → 주석 제거 → `<img>`로 읽어 캔버스에 3배(3000 × 4500)로
그리고 `toBlob`. 외부 라이브러리 없음.

**함정 1:** 직렬화된 SVG는 XML로 파싱된다. 주석 안 연속 하이픈(`--`)이 있으면 저장이
통째로 실패한다. `stripComments()`가 방어하지만 SVG 주석에 `---` 구분선을 쓰지 말 것.
**함정 2:** `<image href="art/…">` 는 파일 참조라 저장본에 담기지 않는다. `inlineArt()` 를
거치지 않으면 음식 그림이 저장본에서만 사라진다.

### 텍스트 배치
글자 폭을 `getComputedTextLength()`로 재서 처리한다.
- `fitText(el, maxW, base)` — 폭을 넘으면 font-size를 줄인다. **반드시 `el.style.fontSize`로**
  쓴다. CSS 클래스로 고정하면 `fitText`가 못 이긴다.
- `lineBlock()` — `/` 로 나눈 손글씨 여러 줄. `max` 로 줄 수를 제한한다.
- **호는 `arcPath(sel, len, angle, apexY)` 하나가 만든다.** 길이에 맞춰 **반지름을 바꿔**
  각도를 고정하는 방식이다 — 페어명 `NAME_ARC` 21°.
  경로를 고정해 두면 짧은 글자는 평평한 가운데에만 놓이고 긴 글자만 휘어 곡률이 들쭉날쭉해진다.
- **초상 이름·페어명·아치 문구는 전부 호 위의 `textPath`**다. 호를 만드는 규칙은 위와 같다.
- **페어명 그림자는 통색이어야 한다.** 그림자에도 종이색 테두리를 두르면 본 글자의 테두리가
  그림자를 갉아먹어 초록 조각이 흩어진 것처럼 보인다. 실제로 그 버그가 있었다.
  본 글자만 `s-paper` 테두리 12, 그림자는 테두리 없이 (9, 15) 만큼 밀어 둔다.
- 한글일 때는 크기를 줄인다(224 → 162). Jua 의 글자통이 라틴 슬랩보다 높고 폭도 넓어서,
  같은 크기로 두면 띠에 닿고 오른쪽 끝이 배지를 덮는다.
- `pill()` — 글자 폭에 맞춘 알약. 간판 띠와 `MENU` 알약이 같이 쓴다.
- 섹션 머리(`drawHead`)는 잰 글자 폭에 여백을 더해 모양의 폭을 잡는다.

### 가운데 그림 액자 = 영수증 (켜고 끄기)
`S.frame` 이 켜지면 **위아래가 톱니로 뜯긴 영수증 종이**(`receiptPath()`, 톱니 22개 · 깊이 10)가
깔리고 사진은 `PHOTO_INSET`(32)만큼 안으로 들어간다. 네 겹이다.
- `#photoMatSh` 같은 모양을 (5,6) 밀어 옅은 먹색으로 — 영수증이 종이에서 떠 보인다
- `#photoMat` 종이색 채움 + 붉은 테두리 3
- `#photoPerf` 톱니 바로 아래를 가로지르는 **점선**(`stroke-dasharray 2 7`)
- `#photoKey` 사진 둘레의 붉은 괘선

**좌우는 곧게, 위아래만 톱니.** 영수증은 롤에서 뜯은 종이라 그렇다. 아랫변은 메뉴 틀에 가려
안 보이므로 결과적으로 "메뉴판에 끼워 둔 영수증"으로 읽힌다 — 이게 다이너 컨셉과 붙는 지점이다.
**점선 한 줄이 영수증이라는 신호의 대부분을 한다.** 지우지 말 것.

물결(스캘럽) 매트를 먼저 만들었다가 걷어냈다 — 봉우리·겹선을 아무리 맞춰도 도일리처럼 보여
다이너에서 따로 놀았다. 겹선을 작은 상자에 다시 그리면 위상이 어긋난다는 것도 그때 배웠다.
- 배경이 투명한 그림은 액자 없이 종이에 인쇄된 것처럼 보이지만, **배경이 있는 사진은 액자가 있어야**
  종이 위에 붕 뜨지 않는다. 그래서 기본값은 끔이고 사용자가 켠다.
- **얇은 이중 괘선 액자는 쓰지 말 것.** 원형 초상의 테두리와 굵기·질감이 같아 부딪혀 보였다.
  지금은 뜯긴 윗변과 점선이 있어 좌우가 곧아도 초상 테두리와 헷갈리지 않는다.
- **켜도 바깥 크기(258~742 / 414~854)는 그대로다.** 종이가 밖으로 자라면 초상 원과 부딪히므로,
  톱니 끝을 상자 선에 맞추고 사진 쪽을 안으로 밀었다.
- 액자는 사진과 같은 층이라 **아랫변은 메뉴 틀에 가려 잘린다.**

### 사진 세 자리
`FRAMES` 에 가운데 그림(484 × 440)과 원형 초상 2장(176 × 176 사각 박스 안의 r88 원)이
같은 모양으로 들어 있다(가운데 464×440 → 484×440, 초상 196 → 176). 가운데 그림만은 액자를 켜면 상자가 안으로 들어가므로 `render()` 가
`PHOTO_BOX` 에서 다시 계산해 `FRAMES.main.box` · `clip-photo` · `#photoHit` 에 함께 써넣는다.
가운데 그림의 `hit` 사각형은 **메뉴 틀에 가리지 않는 부분만**
덮는다(414~774, 높이 360). 가려진 데를 끌 수는 없기 때문이다. `box` 값은 index.html 의 `clip-path` / hit 사각형과
**반드시 같아야 한다.** `placeFrame()` 하나가 셋을 다 처리하고, cover 방식으로 채운 뒤
프레임을 벗어나지 않게 클램프한다.

### 사진·스티커 표시 (중요)
`<image>`의 `display`는 **반드시 `'inline'` / `'none'` 값으로** 지정한다.
`style.display = ''`로 인라인 스타일을 지우면 스타일시트 규칙이 다시 이겨버린다.
실제로 `style.css`의 `display:none` 때문에 화면에서는 사진이 안 보이고 저장본에서만
보이는 버그가 있었다(저장본은 외부 CSS가 안 붙는다).

### 파일 선택
`<label>` 안에 파일 `<input>`을 겹쳐 두면 모바일에서 선택창이 두 번 열리거나 안 열린다.
숨긴 `<input>` 두 개(사진용 `#in-photo`, 스티커용 `#in-sticker`)를 두고 버튼에서
`.click()` 하며, 어느 자리를 눌렀는지 `pendingChar` / `pendingSlot` 에 기억한다.

### 사진·스티커 조작
투명한 히트 사각형에서만 포인터 이벤트를 받는다. `bindGesture()` 하나로 끌기(이동)와
두 손가락·휠(확대)을 함께 처리한다. 사진 1~4배, 음식 그림 0.3~3배.

**기본 음식 그림은 잠겨 있다.** `active: () => !!S.stickers[k].src` — 사용자가 교체한
그림만 움직인다. 기본 그림이 손만 대도 밀리면 거슬린다.

**모바일 사파리는 SVG 요소의 `touch-action`을 무시할 때가 있다.** 그래서 끄는 동안
`dragging` 플래그를 세우고 포스터의 `touchmove`를 `preventDefault()` 한다.

### 업로드 이미지
가운데 그림 2000px / 원형 초상 1100px / 음식 그림 900px로 줄여서 data URI로 담는다
(투명 유지를 위해 전부 PNG). 원본을 그대로 두면 저장이 느려지고 용량을 넘긴다.

### 편집 가능한 항목
페어 이름 · 띠 문구 · 알약 문구 / 가운데 그림(액자 켜기/끄기 포함) /
캐릭터 01·02 (원형 사진 · 이름) / 오른쪽 위 배지 문구(`/` 로 줄바꿈) /
스페셜 페어 세트 1줄(문구·가격) / 음식 그림 3종 교체 / 색상 4종.

바닥 장식 손글씨(`Thanks for stopping by!`)는 **고정값**이다.
편집 패널을 늘리지 않으려고 SVG 에 그대로 박아 뒀다.

### 상태 저장
전체 상태를 `localStorage`(`pairframe.v4`)에 넣는다. 입력할 때마다 수 MB를 직렬화하지
않도록 400ms 디바운스를 걸었고, 용량 초과 시 이미지를 뺀 축약본으로 재시도한다.

## 지나온 길 (다시 밟지 말 것)
포스터를 여러 번 갈아엎었다. `v1-diner-menu` 태그에 이전 형태가 남아 있다.

- **캐릭터 카드 2장 → 메뉴 항목 2열 → 사원증 2장 → 공유 행 표 → 지금.**
  앞의 셋은 전부 `A + B`(따로 노는 두 오브젝트)로 읽혔고, 넷째는 반대로 **표**가 됐다.
  전폭 괘선 + 점선 리더 + 완전 좌우 대칭은 "정보 시트"의 신호다.
- 관계를 **설명하려고** 하트 메달·세로 칸막이·목걸이 끈을 덧붙였지만, 설명은 배치가 못 하는
  일을 대신하지 못했다. 지금은 두 원형 초상이 가운데 그림을 사이에 두고 마주 보는 구도로
  해결했다.
- 음식을 도형 결합으로 그렸다가 전부 걷어냈다. 지금은 손으로 그린 PNG를 쓴다.

## 코딩 시 반드시 지킬 규칙 (사용자 고정 선호)
1. 요청한 수정사항을 적용한 뒤, 전체 코드에 다른 문제가 없는지 검토하고 나서 답변할 것
2. 어디가 문제였고 어디를 고쳤는지 명확하게 설명할 것
3. 요청받지 않은 다른 부분은 절대 임의로 수정하지 말 것

## 현재 상태
- Playwright로 데스크톱/모바일 렌더, 사진 3장 업로드, 음식 그림 교체·복귀, 한글 자동 전환,
  긴 문구 오버플로 검사, 3000×4500 PNG 저장(그림·폰트 임베드 확인), 새로고침 복원,
  전체 초기화까지 실제로 확인함.
- 배포는 아직 안 함. 정적 호스팅에 그대로 올리면 된다.

## 앞으로 확장 여지 (요청 있을 때만)
- 메뉴 섹션 제목 편집
- 종이 질감을 `feTurbulence` 로 더 주기 (지금은 점 얼룩만)
- 스티커 회전 / 슬롯 추가
- 저장 배율 선택 (지금은 3배 고정)
- 템플릿 여러 종
